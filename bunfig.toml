[test]
# Order matters: the DOM must exist before @vue/runtime-dom is first imported,
# since it captures `document` at module scope.
preload = [
  "./tests/support/dom.ts",
  "./tests/support/vue-plugin.ts",
  "./tests/support/setup.ts",
]
